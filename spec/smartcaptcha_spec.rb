# frozen_string_literal: true

RSpec.describe Smartcaptcha do
  it "has a version number" do
    expect(Smartcaptcha::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
